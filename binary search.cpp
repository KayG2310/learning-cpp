#include<bits/stdc++.h>
using namespace std;

int main(){
    vector<int> v = {4, 8, 15, 16, 27, 34, 42,46,50);
    int num, length, mid, index, check;
    num = 42;
    length = v.size();
    mid = length/2;
    index = -1;
    check = mid -1;
}
